# 1. 장고 코어 (버전 명시 권장)
Django>=5.0,<5.1

# 2. 웹 서버 인터페이스 (운영 서버용 WSGI)
gunicorn==21.2.0

# 3. 데이터베이스 어댑터 (PostgreSQL 사용 시 필수)
psycopg2-binary==2.9.9

# 4. 환경 변수 관리 (비밀키 등 보안 설정 분리용)
python-dotenv==1.0.1

# 5. 정적 파일 서빙 최적화 (CSS/JS 파일 처리용)
whitenoise==6.6.0

# 6. 파이썬용 MySQL 드라이버, WhiteNoise 라이브러리 설치 추가
Django>=4.2
mysqlclient>=2.2.0
whitenoise>=6.6.0